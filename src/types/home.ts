export type TCharacters = {
    image: string,
    status:string,
    species:string
    }[]
    export type TCharacter = {
        image: string,
        status:string,
        species:string
        }